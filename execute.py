from apex_rp_analyse import *

nb_kills = 0
nb_participation = 0
placement = 1
tier = apexRes.ApexRankTier.MASTER
division = apexRes.ApexRankDiv.IV
total_rp = 15000
SEASON = 13

#stats(nb_kills, nb_participation, placement, tier, division)

#compare_stats(nb_kills, nb_participation, placement, tier, division)

#check_stats_ranked(nb_kills, nb_participation, placement)

#check_stats_ranked_kill_range(0)

#check_min_placement(nb_kills, nb_participation)

#check_stats_ranked_kill_range_per_tier(nb_participation, tier, division)

#compare_stats2(nb_kills, nb_participation, placement, tier, division)

#comp_check_stats_ranked(nb_kills, nb_participation, placement)

#compare_min_placement(nb_kills, nb_participation)

#check_min_placement_per_tier(nb_kills, nb_participation, tier, division)

#check_min_placement_per_tier(nb_kills, nb_participation, 
#                             apexRes.ApexRankTier.MASTER, 
#                             apexRes.ApexRankDiv.IV)